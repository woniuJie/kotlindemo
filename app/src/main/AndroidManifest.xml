<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kotlindemo">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".MyApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:hardwareAccelerated="false"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".countdowntime.CountDownTimeActivity"
            android:hardwareAccelerated="true"></activity>
        <activity android:name=".glide.GlideActivity" />
        <activity android:name=".oom.OOMActivity" />
        <activity android:name=".async.AsyncActivity" />
        <activity android:name=".viewpager.ViewPagerActivity" />
        <activity android:name=".event.EventActivity" />
        <activity android:name=".maoboli.MaoBoLiActivity" />
        <activity android:name=".xiecheng.XieChengActivity" />
        <activity android:name=".dianzan.DianzanActivity" />
        <activity android:name=".network.DemoActivity" />
        <activity android:name=".SecondActivity" />
        <activity android:name=".FirstActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>